#include <set>
#include <vector>
#include <queue>
#include <tuple>
using namespace std;

using Coords = pair<int, int>;

struct Node {
    int value;
    Coords coords;
    Node(int x, int y) : value(-1), coords(make_pair(x, y)) {}
};


int main() {

}